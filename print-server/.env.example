# Porta HTTP del server di stampa (default: 3001)
PORT=3001

# Nome del server mostrato nei log
PRINT_SERVER_NAME=ESC/POS Print Server

# Le stampanti fisiche si configurano direttamente in printers.config.js
# Non sono necessarie variabili d'ambiente per la connessione alle stampanti.
